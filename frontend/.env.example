# Copy this file to .env.local and fill in your values
# The API URL is the output from `sam deploy` — look for ApiEndpoint in Outputs

VITE_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod
